services:
  - type: web
    name: web-p90
    env: python
    region: singapore                # hoặc oregon, frankfurt, ... tùy Render
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true                 # 🔥 Tự động deploy khi push GitHub
    envVars:
      - key: GOOGLE_CREDS
        sync: false                  # giữ bí mật (Render lưu thủ công)
    plan: free                       # hoặc starter / pro nếu bạn dùng gói khác
